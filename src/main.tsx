import { useMemo, useState, type ReactNode } from 'react';
import { createRoot } from 'react-dom/client';
import {
  Archive, ArrowUpRight, Bell, BookOpen, Box, Check, CheckCircle2, ChevronDown,
  Clapperboard, Clock3, FileText, FolderOpen, Home, Image, Lightbulb,
  Menu, Mic2, MoreHorizontal, Music2, Play, Plus, Search, Settings2, Sparkles,
  Target, Users, Video, WandSparkles, X, Zap,
} from 'lucide-react';
import './index.css';

type Status = 'Ideas' | 'Scripts' | 'In production' | 'Quality check' | 'Published';
type Episode = { id: string; title: string; lesson: string; category: string; status: Status; progress: number; duration: string; accent: string; emoji: string };

const episodes: Episode[] = [
  { id: 'EP01', title: 'Leo Learns to Share', lesson: 'Sharing', category: 'Life lesson', status: 'In production', progress: 68, duration: '3:10', accent: 'coral', emoji: '🧸' },
  { id: 'EP02', title: 'Leo Goes to School', lesson: 'Trying something new', category: 'Adventure', status: 'Scripts', progress: 31, duration: '2:45', accent: 'sky', emoji: '🎒' },
  { id: 'EP03', title: "Leo Learns His ABCs", lesson: 'Alphabet', category: 'Education', status: 'Ideas', progress: 12, duration: '3:30', accent: 'lilac', emoji: '🔤' },
  { id: 'EP04', title: 'Leo Counts to 10', lesson: 'Numbers', category: 'Education', status: 'Ideas', progress: 8, duration: '2:20', accent: 'gold', emoji: '🔢' },
  { id: 'EP05', title: 'Leo Helps a Friend', lesson: 'Helping others', category: 'Life lesson', status: 'Published', progress: 100, duration: '3:00', accent: 'mint', emoji: '🤝' },
  { id: 'EP06', title: 'Leo Learns Colors', lesson: 'Colors', category: 'Education', status: 'Quality check', progress: 89, duration: '2:55', accent: 'peach', emoji: '🌈' },
];

const navItems = [
  { label: 'Studio overview', icon: Home },
  { label: 'Episodes', icon: Clapperboard, count: '06' },
  { label: 'Characters', icon: Users },
  { label: 'Locations', icon: FolderOpen },
];
const assetItems = [
  { label: 'Visual assets', icon: Image },
  { label: 'Voice cast', icon: Mic2 },
  { label: 'Music library', icon: Music2 },
];

function App() {
  const [active, setActive] = useState('Studio overview');
  const [selectedEpisode, setSelectedEpisode] = useState<Episode | null>(null);
  const [showNotifications, setShowNotifications] = useState(false);
  const [search, setSearch] = useState('');
  const visibleEpisodes = useMemo(() => episodes.filter((episode) => `${episode.title} ${episode.lesson} ${episode.category}`.toLowerCase().includes(search.toLowerCase())), [search]);

  const openEpisode = (episode: Episode) => { setSelectedEpisode(episode); setActive('Episodes'); };
  const isEpisodeView = active === 'Episodes' || selectedEpisode;

  return (
    <div className="app-shell">
      <aside className="sidebar">
        <div className="brand"><div className="brand-mark"><span>✦</span></div><div><strong>Leo & Friends</strong><small>Studio workspace</small></div></div>
        <div className="workspace-switcher"><div className="workspace-icon">LF</div><div><strong>Leo & Friends</strong><small>Content studio</small></div><ChevronDown size={14} /></div>
        <div className="side-label">Workspace</div>
        <nav className="side-nav">{navItems.map(({ label, icon: Icon, count }) => <button key={label} className={active === label ? 'active' : ''} onClick={() => { setActive(label); setSelectedEpisode(null); }}><Icon size={17} /><span>{label}</span>{count && <em>{count}</em>}</button>)}</nav>
        <div className="side-label">Asset library</div>
        <nav className="side-nav">{assetItems.map(({ label, icon: Icon }) => <button key={label} className={active === label ? 'active' : ''} onClick={() => setActive(label)}><Icon size={17} /><span>{label}</span></button>)}</nav>
        <div className="sidebar-bottom"><div className="studio-card"><div className="studio-card-icon"><Sparkles size={15} /></div><strong>Keep the magic consistent.</strong><p>Reuse your characters, worlds and sounds in every episode.</p><button onClick={() => setActive('Characters')}>Open character bible <ArrowUpRight size={13} /></button></div><div className="profile"><div className="avatar">DA</div><div><strong>David Amukoche</strong><small>Studio owner</small></div><MoreHorizontal size={17} /></div></div>
      </aside>
      <main className="main-content">
        <header className="topbar"><div className="mobile-brand"><Menu size={19} /><span>Leo & Friends</span></div><div className="breadcrumbs"><span>Workspace</span><span>/</span><strong>{selectedEpisode ? selectedEpisode.id : active}</strong></div><div className="top-actions"><button className="icon-btn" onClick={() => setShowNotifications(!showNotifications)}><Bell size={17} /><i /></button><div className="top-avatar">DA</div>{showNotifications && <div className="notification-popover"><strong>Studio activity</strong><p>EP01 scene 6 is ready for review.</p><small>2 minutes ago</small></div>}</div></header>
        {selectedEpisode ? <EpisodeDetail episode={selectedEpisode} onBack={() => setSelectedEpisode(null)} /> : isEpisodeView ? <EpisodesView episodes={visibleEpisodes} search={search} setSearch={setSearch} onOpen={openEpisode} /> : active === 'Characters' ? <CharactersView /> : active === 'Locations' ? <LocationsView /> : active === 'Visual assets' || active === 'Voice cast' || active === 'Music library' ? <LibraryView active={active} /> : <Overview onOpen={openEpisode} />}
      </main>
    </div>
  );
}

function Overview({ onOpen }: { onOpen: (episode: Episode) => void }) {
  return <div className="page-wrap"><div className="page-heading"><div><div className="eyebrow">Sunday, 20 September 2026</div><h1>Good morning, David <span>✦</span></h1><p>Your next little adventure is taking shape.</p></div><button className="primary-btn" onClick={() => onOpen(episodes[0])}><Plus size={16} />New episode <ChevronDown size={14} /></button></div>
    <section className="hero-card"><div className="hero-copy"><div className="eyebrow lime">THIS WEEK IN THE STUDIO</div><h2>One excellent episode<br /><em>unlocks the next 100.</em></h2><p>Keep the production loop simple: story, scenes, voices, then a little sparkle.</p><button className="hero-btn" onClick={() => onOpen(episodes[0])}>Continue EP01 <ArrowUpRight size={15} /></button></div><div className="hero-illustration"><div className="sun" /><div className="cloud cloud-one" /><div className="cloud cloud-two" /><div className="hill hill-one" /><div className="hill hill-two" /><div className="lion-face"><div className="ear left" /><div className="ear right" /><div className="mane" /><div className="face"><div className="eye left-eye" /><div className="eye right-eye" /><div className="nose" /><div className="smile" /></div></div><div className="backpack" /></div></section>
    <section className="section-row"><div><div className="eyebrow">THE PIPELINE</div><h2 className="section-title">At a glance</h2></div><button className="text-btn">View production board <ArrowUpRight size={14} /></button></section>
    <section className="stats-grid"><Stat icon={<Clapperboard />} label="Episodes in flight" value="06" note="2 moving this week" tone="mint" /><Stat icon={<Clock3 />} label="Scenes to review" value="14" note="Across 3 episodes" tone="peach" /><Stat icon={<CheckCircle2 />} label="Published episodes" value="01" note="First one is live" tone="lilac" /><Stat icon={<Target />} label="Studio consistency" value="92%" note="Character design match" tone="sky" /></section>
    <section className="content-grid"><div className="panel"><div className="panel-head"><div><h2>Production board</h2><p>Move every story from spark to screen.</p></div><button className="icon-btn small"><MoreHorizontal size={16} /></button></div><div className="board-grid">{(['Ideas', 'Scripts', 'In production', 'Quality check'] as Status[]).map(status => <div className="board-column" key={status}><div className="board-column-head"><span>{status}</span><b>{episodes.filter(e => e.status === status).length}</b></div>{episodes.filter(e => e.status === status).slice(0, 2).map(episode => <EpisodeCard key={episode.id} episode={episode} onClick={onOpen} />)}</div>)}</div></div><div className="panel activity-panel"><div className="panel-head"><div><h2>Studio rhythm</h2><p>Small steps, lovely momentum.</p></div><Zap size={17} className="accent-icon" /></div><div className="activity-list"><Activity icon={<Check size={12} />} title="EP05 published to YouTube" time="Today, 09:40 AM" tone="mint" /><Activity icon={<Video size={13} />} title="Scene 6 rendered for EP01" time="Yesterday, 4:18 PM" tone="sky" /><Activity icon={<Mic2 size={13} />} title="Leo voice take approved" time="Yesterday, 2:05 PM" tone="peach" /><Activity icon={<Lightbulb size={13} />} title="New idea added to the library" time="18 Sep, 11:22 AM" tone="lilac" /></div><div className="activity-footer"><WandSparkles size={15} /><span>Every story starts with a tiny spark.</span></div></div></section>
  </div>;
}

function EpisodesView({ episodes: list, search, setSearch, onOpen }: { episodes: Episode[]; search: string; setSearch: (value: string) => void; onOpen: (episode: Episode) => void }) {
  return <div className="page-wrap"><div className="page-heading"><div><div className="eyebrow">CONTENT CALENDAR</div><h1>Episodes</h1><p>Six stories in the Leo & Friends universe.</p></div><button className="primary-btn" onClick={() => onOpen(episodes[0])}><Plus size={16} />New episode</button></div><div className="filter-bar"><div className="search-field"><Search size={16} /><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search episodes, lessons or categories" /></div><button className="filter-btn">All statuses <ChevronDown size={14} /></button><button className="filter-btn">All categories <ChevronDown size={14} /></button></div><div className="episode-list">{list.map(episode => <EpisodeRow key={episode.id} episode={episode} onClick={onOpen} />)}</div></div>;
}

function EpisodeDetail({ episode, onBack }: { episode: Episode; onBack: () => void }) {
  const scenes = ['The toy box surprise', 'A tricky little question', 'Leo tries to keep it all', 'Koko has an idea', 'A sharing song', 'The happy hand-off', 'Friends play together', 'The lesson repeat'];
  return <div className="page-wrap detail-page"><button className="back-link" onClick={onBack}>← Back to episodes</button><div className="detail-heading"><div><div className="eyebrow">{episode.id} · {episode.category.toUpperCase()}</div><h1>{episode.title}</h1><p>Production specification · Target age 3–6 · {episode.duration}</p></div><div className="detail-actions"><button className="secondary-btn"><Play size={15} />Preview</button><button className="primary-btn"><Check size={15} />Mark ready</button></div></div><div className="detail-grid"><section className="detail-main"><div className="episode-hero"><div className={`episode-art ${episode.accent}`}><span>{episode.emoji}</span><div className="art-sun" /><div className="art-hill" /></div><div className="episode-summary"><span className={`status-pill ${episode.status.toLowerCase().replaceAll(' ', '-')}`}>{episode.status}</span><h2>Lesson: <em>{episode.lesson}</em></h2><p>Leo discovers that sharing makes playtime bigger, brighter and more fun for everyone.</p><div className="progress-line"><div><span>Overall progress</span><strong>{episode.progress}%</strong></div><div className="progress-track"><i style={{ width: `${episode.progress}%` }} /></div></div></div></div><div className="panel scene-panel"><div className="panel-head"><div><h2>Scene checklist</h2><p>8 visual scenes · keep Leo consistent in every frame.</p></div><button className="text-btn"><Plus size={14} />Add scene</button></div><div className="scene-list">{scenes.map((scene, index) => <div className="scene-row" key={scene}><div className={`scene-number ${index < 5 ? 'done' : ''}`}>{index < 5 ? <Check size={13} /> : index + 1}</div><div><strong>Scene {index + 1} · {scene}</strong><small>{index < 5 ? 'Visual prompt complete · audio attached' : 'Ready for visual direction'}</small></div><span className={index < 5 ? 'scene-done' : 'scene-next'}>{index < 5 ? 'Complete' : 'Next'}</span><MoreHorizontal size={16} /></div>)}</div></div></section><aside className="detail-side"><div className="panel"><div className="panel-head"><div><h2>Production package</h2><p>Episode 1 starter kit</p></div><Box size={17} className="accent-icon" /></div><div className="package-list">{['Character bible', 'Episode script', '8 scene descriptions', 'Image / animation prompts', 'Narrator script', 'Character dialogue', 'Music direction', 'Sound effects', 'Editing timeline', 'Thumbnail prompt'].map((item, index) => <div key={item}><span className={index < 7 ? 'checkmark' : 'empty-check'}>{index < 7 ? <Check size={11} /> : ''}</span><span>{item}</span><small>{index < 7 ? 'Ready' : 'To do'}</small></div>)}</div><button className="wide-btn">Open production package <ArrowUpRight size={14} /></button></div><div className="panel lesson-panel"><div className="eyebrow">REPEATABLE FORMULA</div><h3>Story + Character + Lesson + Repetition + Music</h3><p>Keep the message clear, playful and positive.</p><div className="formula-dots"><i /><i /><i /><i /><i /></div></div></aside></div></div>;
}

function CharactersView() { const characters = [{ name: 'Leo', role: 'Main character', desc: 'Curious, energetic and kind.', emoji: '🦁', accent: 'coral' }, { name: 'Mia', role: 'Question asker', desc: 'Curious and clever.', emoji: '🐰', accent: 'sky' }, { name: 'Koko', role: 'Funny friend', desc: 'Playful and full of surprises.', emoji: '🐼', accent: 'lilac' }, { name: 'Tiki', role: 'Adventure guide', desc: 'Energetic and brave.', emoji: '🐦', accent: 'gold' }, { name: 'Toto', role: 'Problem solver', desc: 'Calm and thoughtful.', emoji: '🐢', accent: 'mint' }]; return <div className="page-wrap"><div className="page-heading"><div><div className="eyebrow">THE LEO & FRIENDS UNIVERSE</div><h1>Character bible</h1><p>Keep every friend recognizable, episode after episode.</p></div><button className="primary-btn"><Plus size={16} />Add character</button></div><div className="character-grid">{characters.map(character => <div className={`character-card ${character.accent}`} key={character.name}><div className="character-art"><span>{character.emoji}</span></div><div className="character-info"><span className="role-label">{character.role}</span><h2>{character.name}</h2><p>{character.desc}</p><button className="text-btn">View design notes <ArrowUpRight size={13} /></button></div></div>)}</div><div className="consistency-banner"><div className="consistency-icon"><Sparkles size={19} /></div><div><strong>Master visual prompt is locked in.</strong><p>Young cartoon lion · golden-orange fur · blue T-shirt · yellow backpack · soft 3D preschool animation.</p></div><button className="secondary-btn">Copy prompt</button></div></div>; }
function LocationsView() { const locations = [{ name: "Leo's House", desc: 'Warm home base', emoji: '🏡', count: '06 scenes' }, { name: 'Sunshine Park', desc: 'Main outdoor location', emoji: '🌳', count: '12 scenes' }, { name: 'Little Stars School', desc: 'Learning episodes', emoji: '🏫', count: '04 scenes' }, { name: 'Wonder Woods', desc: 'Adventure stories', emoji: '🌲', count: '03 scenes' }, { name: 'Rainbow Meadow', desc: 'Songs and imagination', emoji: '🌈', count: '05 scenes' }, { name: 'Dreamy Moon Valley', desc: 'Bedtime episodes', emoji: '🌙', count: '02 scenes' }]; return <div className="page-wrap"><div className="page-heading"><div><div className="eyebrow">REUSABLE WORLDS</div><h1>Locations</h1><p>Simple, safe environments that make production faster.</p></div><button className="primary-btn"><Plus size={16} />Add location</button></div><div className="location-grid">{locations.map(location => <div className="location-card" key={location.name}><div className="location-emoji">{location.emoji}</div><div><h2>{location.name}</h2><p>{location.desc}</p><span>{location.count}</span></div><ArrowUpRight size={16} /></div>)}</div></div>; }
function LibraryView({ active }: { active: string }) { return <div className="page-wrap"><div className="page-heading"><div><div className="eyebrow">ASSET LIBRARY</div><h1>{active}</h1><p>Reusable building blocks for the next story.</p></div><button className="primary-btn"><Plus size={16} />Add asset</button></div><div className="empty-library"><div className="empty-icon"><Archive size={24} /></div><h2>Your {active.toLowerCase()} will live here.</h2><p>Start with a small, reusable library and let each episode get faster.</p><button className="secondary-btn"><WandSparkles size={15} />Create first asset</button></div></div>; }

function EpisodeCard({ episode, onClick }: { episode: Episode; onClick: (episode: Episode) => void }) { return <button className="episode-card" onClick={() => onClick(episode)}><div className={`card-art ${episode.accent}`}><span>{episode.emoji}</span></div><div className="card-body"><div className="card-top"><span>{episode.id}</span><MoreHorizontal size={14} /></div><strong>{episode.title}</strong><small>{episode.lesson}</small><div className="mini-progress"><i style={{ width: `${episode.progress}%` }} /></div><div className="card-meta"><span>{episode.progress}% ready</span><span>{episode.duration}</span></div></div></button>; }
function EpisodeRow({ episode, onClick }: { episode: Episode; onClick: (episode: Episode) => void }) { return <button className="episode-row" onClick={() => onClick(episode)}><div className={`row-art ${episode.accent}`}>{episode.emoji}</div><div className="row-title"><strong>{episode.id} · {episode.title}</strong><small>{episode.category} · Lesson: {episode.lesson}</small></div><span className={`status-pill ${episode.status.toLowerCase().replaceAll(' ', '-')}`}>{episode.status}</span><div className="row-progress"><div className="mini-progress"><i style={{ width: `${episode.progress}%` }} /></div><small>{episode.progress}%</small></div><span className="row-duration">{episode.duration}</span><ArrowUpRight size={15} /></button>; }
function Stat({ icon, label, value, note, tone }: { icon: ReactNode; label: string; value: string; note: string; tone: string }) { return <div className="stat-card"><div className={`stat-icon ${tone}`}>{icon}</div><span className="stat-label">{label}</span><strong className="stat-value">{value}</strong><span className="stat-note">{note}</span></div>; }
function Activity({ icon, title, time, tone }: { icon: ReactNode; title: string; time: string; tone: string }) { return <div className="activity-item"><div className={`activity-icon ${tone}`}>{icon}</div><div><strong>{title}</strong><small>{time}</small></div></div>; }

createRoot(document.getElementById('root')!).render(<App />);
